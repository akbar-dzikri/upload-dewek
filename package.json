{
  "name": "upload-dewek",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --config wrangler.local.jsonc",
    "deploy": "wrangler deploy --minify",
    "deploy:local": "wrangler deploy --minify --config wrangler.local.jsonc",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "cf-typegen:local": "wrangler types --env-interface CloudflareBindings --config wrangler.local.jsonc",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply",
    "db:migrate:local": "wrangler d1 migrations apply --local",
    "format": "prettier --write .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "test:e2e": "vitest run tests/e2e.live.test.ts --reporter=verbose",
    "test:e2e:live": "E2E_BASE_URL=https://upload-dewek.dikicodes.com E2E_API_KEY=ud_local_test_key_12345 vitest run tests/e2e.live.test.ts --reporter=verbose"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "aws4fetch": "^1.0.20",
    "drizzle-orm": "^0.45.2",
    "drizzle-zod": "^0.8.3",
    "hono": "^4.13.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@eslint/js": "^9.37.0",
    "@types/better-sqlite3": "^9.6.0",
    "@vitest/coverage-v8": "^4.1.11",
    "better-sqlite3": "^13.0.3",
    "drizzle-kit": "^0.31.9",
    "eslint": "^9.37.0",
    "globals": "^16.4.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.1",
    "vitest": "^4.1.11",
    "wrangler": "^4.4.0"
  }
}
